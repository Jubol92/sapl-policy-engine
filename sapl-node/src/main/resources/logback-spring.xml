<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Loaded by Spring Boot's LoggingSystem — replaces logback.xml for server mode.
         Includes Spring Boot's standard console format (colors, timestamps, etc.).
         logging.level properties from application.yml are applied on top of this. -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
